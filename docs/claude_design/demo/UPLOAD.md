# 上传清单 — 过程演示 / 流水线透视台（Claude Design）

把**本文件夹内全部文件**一起上传给 Claude Design，然后粘贴 `design_handoff_demo.md` 里的 Kickoff Prompt。

| 文件 | 作用 | Hero 屏必需 |
|---|---|---|
| `design_handoff_demo.md` | 设计简报 + Kickoff Prompt + 深色科技 Token + 各节点要明文显示的字段清单 + 迭代顺序 | ✅ |
| `sample_state.json` | glm-5.1 真实运行的精简数据，用于真实渲染节点字段 | ✅ |

**建议**：先只做 Hero（14 节点实时流水线 DAG + 播放动画 + 计数侧栏），满意后再扩展节点检视抽屉与断点交互面板。
