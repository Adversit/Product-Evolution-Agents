# docs/assets

README 用的视觉素材（hero GIF / 截图）。

| 文件 | 用途 | 来源 |
|---|---|---|
| `observatory.gif` | 根 README + frontend/README 顶部 hero：透视台播放动画 | 浏览器录制 `frontend` 运行态 |
| `observatory-drawer.png` | 前端可视化章节：节点检视抽屉截图 | 同上 |

录制方法：`cd frontend && npm run dev` → 打开 `http://localhost:5173` → 点 ▶ 播放 → 录屏导出。
